# Day 01：マルチエージェントレビューの実際の価値

**日付:** 2026-05-24
**フェーズ:** Phase 0（初期構成）

---

## 何が起きたか

JPYC のコントラクトアドレスをコードに追加した際、末尾を誤って記述してしまった。

```java
// 誤り（設定した値）
JPYC("0x431D5dfF03120AFA4bDf332c61A6e1766eF37BF9", 18)

// 正しい値（レビューエージェントが検出）
JPYC("0x431D5dfF03120AFA4bDf332c61A6e1766eF37BDB", 18)
```

末尾3文字 `BF9` → `BDB` の違い。目視では見つけにくいレベルの誤り。

---

## なぜ重要だったか

Ethereum のアドレスは 42 文字の16進数。1文字でも違えば**全く別のアドレス**になる。

もしこの誤ったアドレスで本番稼働した場合：
- ユーザーが JPYC を送金しても、システム側が監視しているアドレスが間違っているため**入金検知が永久に起きない**
- 結果として「送金は完了しているのに決済が確認されない」という状態になる

コントラクトアドレスの1文字ミスは、バグではなく**入金の永久未検知**に直結する。

> **補足:** 「誤ったアドレスにトークンが送金されて消失する」わけではない。ERC-20トークンはユーザーのウォレットからウォレットへ `transfer()` で送られるため、システム側の監視アドレスが違っていても送金先は変わらない。問題は「正しく届いたのに検知できない」こと。また、この `BF9`→`BDB` の誤りは16進数の値そのものが異なるため、EIP-55チェックサム検証でも事前に防げない種類のミスだった。

---

## マルチエージェントレビューが機能した理由

ブロックチェーン専門エージェントが Etherscan で実際のアドレスを照合し、コードと一致しないことを検出した。

人間のコードレビューで `BF9` と `BDB` の違いを見つけるのは非常に困難。専門エージェントが「このアドレスを実際に検索してみる」というアプローチを取ったからこそ検出できた。

---

## もう一つの気づき：Infura URL のログ漏洩リスク

Web3jConfig で `new HttpService(clientAddress)` と書くだけだと、OkHttp が起動時に接続先 URL をログに出力するケースがある。

URL の中に Infura の API Key が含まれているため、**ログファイルを見られると API Key が漏れる**。

対策として `HttpLoggingInterceptor.Level.NONE` を設定した。

```java
OkHttpClient httpClient = new OkHttpClient.Builder()
    .addInterceptor(new HttpLoggingInterceptor().setLevel(HttpLoggingInterceptor.Level.NONE))
    .build();
```

これは「コードに API Key を書かない」だけでは防げない類のリスク。**ログへの間接的な漏洩**を常に意識する必要がある。

---

## 得た教訓

| 教訓 | 詳細 |
|---|---|
| コントラクトアドレスは必ず外部ソース（Etherscan）と照合する | 目視確認は信頼できない |
| 秘密情報はコードだけでなくログにも漏れうる | URL・ヘッダー・例外メッセージを疑う |
| 専門エージェントを複数立てる意味がある | 一つの視点では見逃すものがある |
| レビューは PR を出す前にも価値がある | 早く指摘されるほど修正コストが低い |

---

## 今後に活かすこと

- コントラクトアドレスを変更・追加するときは必ずブロックチェーンエージェントに Etherscan 照合を依頼する
- `scripts/security-check.sh` を PR 前に必ず実行する
- 外部サービスとの接続設定（URL・認証情報）はログ出力を明示的に `NONE` にする

---

## 📝 1日の振り返り

### ✅ よかったこと

- マルチエージェントレビューが**初日から実際の問題を検出**した（JPYCアドレスミス・Infura URL漏洩）
- セキュリティ問題をマージ前にすべて修正できた（本番に一切出なかった）
- CLAUDE.md でルールを永続化したことで、今後のセッションでも同じ基準が保たれる
- PR → レビュー → マージのフローを実際に回して動作確認できた
- コントラクトアドレスはパブリック情報、秘密鍵はNG、という区別を明確にできた

### 🔧 改善が必要なこと

- `when-authorized` を設定したが Spring Security 未導入で機能しなかった → 設定と実装の乖離に気をつける
- 最初の `application.yml` に開発設定が混在していた → 最初から dev/prod を分けて設計すべきだった
- `receiverAddress` のバリデーションが初期構成に入っていなかった → セキュリティ要件は設計段階から組み込む

### ⚠️ 注意するべきこと

- **コントラクトアドレスは1文字ミスで資金喪失**。変更・追加時は必ず Etherscan 照合を依頼する
- **環境変数のデフォルト値**に本番想定外の値を入れない（H2デフォルト・プレースホルダーURL）
- **ログへの間接漏洩**を意識する。URL・例外メッセージ・リクエストボディに秘密情報が混入しないか常に確認
- `application-dev.yml` が本番に誤ってロードされないようデプロイ手順で `SPRING_PROFILES_ACTIVE` を明示する
